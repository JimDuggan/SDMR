#Create an R vector of squares of 1 to 10
v <- (1:10)^2
#Find the minimum
min <- min(v)
#Find the maximum
#Find the average
#Subset all those values greater 
#than the average

f <- function(x){
  x[x %% 2 == 0]
}


